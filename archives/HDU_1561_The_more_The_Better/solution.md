# 题解

## 思路

本题是一道典型的「树上依赖背包」问题。

依赖关系天然形成一片森林：如果城堡 i 的 prerequisite 是 a，则 a 是 i 的父节点；所有 a = 0 的城堡没有父节点，可以视为直接从虚拟根节点 0 出发。

为了把多棵树合并成一棵树，我们建立一个虚拟根节点 0，它的权值为 0，并向所有 a = 0 的城堡连边。于是问题变成：

> 在这棵有根树中，选择恰好 M+1 个节点（包括虚拟根 0），要求若选择某个节点，则必须选择它的父节点。最大化所选节点的权值和。

设 `f[u][j]` 表示在 u 的子树中，**必须选 u**，一共选 j 个节点能获得的最大宝物数。

- 初始化：`f[u][1] = W[u]`（只选 u 自己）。在本实现中，为了代码简洁，把所有 `f[u][i]` 都初始化为 `W[u]`；由于题目保证 $b \ge 0$，这个下界不会导致最终答案出错。
- 转移：对每个子节点 v，做 0/1 背包合并：
  ```cpp
  for (int j = m + 1; j >= 1; j--)
      for (int k = 1; k <= j - 1; k++)
          f[u][j] = max(f[u][j], f[u][j - k] + f[v][k]);
  ```
  外层从大到小枚举 j，是为了保证每个子节点只被合并一次（0/1 背包）。
- 答案：`f[0][M + 1]`，因为虚拟根 0 必须选，剩下的 M 个节点就是题目要求的 M 个城堡。

## 时间复杂度

对每个节点，做一遍容量为 M 的背包。转移为 $O(M^2)$，总复杂度 $O(N \cdot M^2)$。

$N \le 200, M \le 200$，最大约 $8 \times 10^6$ 次运算，可轻松通过。

## 空间复杂度

`f[N][N]`，约 $200 \times 200 = 4 \times 10^4$ 个整数，加上边表，非常小。

## 代码说明

见 `correct_code.cpp`。
