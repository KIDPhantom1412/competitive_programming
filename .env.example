# Build configuration

# C++ compiler path
CXX=g++

# Compile flags
CXXFLAGS="-std=c++23 -O2 -Wall -Wextra"

# Main-thread stack size in KB (Linux/WSL). Applied by run.sh via ulimit -s.
# Linker flags like -Wl,-z,stack-size= do not change Linux main-thread stack.
ULIMIT_STACK=524288
